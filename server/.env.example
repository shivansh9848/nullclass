# Server Configuration
PORT=5000

# Database Configuration - Replace with your MongoDB connection string
MONGODB_URL=mongodb://localhost:27017/nullclass

# JWT Secret Key - IMPORTANT: Change this to a secure random string in production
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environment
NODE_ENV=development

# Client URL (for password reset links)
CLIENT_URL=http://localhost:3000

# Email Configuration (for password reset emails)
# For Gmail, use your email and an "App Password" (not your regular password)
# Enable 2FA on Gmail and generate an App Password: https://myaccount.google.com/apppasswords
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=cokt cjvx tweo ybwv
EMAIL_FROM=stackoverflow <noreply@nullclass.com>

# Cloudinary Configuration (for file uploads)
# Sign up at https://cloudinary.com and get your CLOUDINARY_URL from your dashboard
# This is the modern method - just one URL instead of separate credentials
CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME

# SMS Configuration (for password reset via SMS)
# Uncomment and configure these when ready to use real SMS sending
# TWILIO_SID=your_twilio_account_sid
# TWILIO_TOKEN=your_twilio_auth_token
# TWILIO_PHONE=your_twilio_phone_number

# Forgot Password Settings
# Rate limiting (hours between password reset requests)
PASSWORD_RESET_RATE_LIMIT_HOURS=24

# New Password Settings
# Generated password length (default: 10 characters)
GENERATED_PASSWORD_LENGTH=10

# Security Settings
# Password reset token expiry (in milliseconds, default: 1 hour)
RESET_TOKEN_EXPIRY=3600000
